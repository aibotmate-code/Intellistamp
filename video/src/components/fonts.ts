// Font families used by IntelliStamp — loaded via @remotion/google-fonts pattern.
// In Remotion these must be declared as CSS font-face or imported via loadFont.
// We use inline style strings so each component can reference them directly.

export const fontDisplay  = "'Bebas Neue', 'Arial Narrow', Arial, sans-serif"
export const fontBody     = "'Barlow', Arial, sans-serif"
export const fontMono     = "'Barlow Condensed', 'Arial Narrow', Arial, sans-serif"
